test/XParsec.FSharp.Tests/data/
bench/XParsec.FSharp.Benchmarks/fixtures/
src/XParsec/Readables.fs
# FSharp.Core port — uses --compiling-fslib / inline-IL constructs Fantomas can't parse.
src/XParsec.FSharp.Lib/
src/Vesper.Core/
# Vesper.List — same `[]`/`::` operator-union-case syntax Fantomas mangles (it
# strips the case payloads `: 'T list` / `Head: 'T * Tail: …`). Authored to a
# fixed shape; parser coverage is the golden `.parsed` snapshots, not Fantomas.
src/Vesper.List/
src/Vesper.Comparison/
# Vesper.Array — `array.fs` opens its `zeroCreate` over the `newarr` inline-IL
# intrinsic (`(# "..." #)`), the same construct Fantomas's FCS backend can't
# parse in Vesper.Core / Vesper.Comparison. Authored to a fixed shape; parser
# coverage is the golden `.parsed` snapshots.
src/Vesper.Array/
# Vesper.Seq — the golden `.parsed` snapshots embed `pos=`/`tok=` offsets, so any
# reformat that shifts characters breaks them. Authored to a fixed shape; parser
# coverage is the golden snapshots, not Fantomas (same stance as the siblings).
src/Vesper.Seq/
# Vesper.Set — verbatim FSharp.Core copy; preserve upstream formatting/comments.
src/Vesper.Set/
