[core]
name = "Vesper.Result"
description = "The result type and the Result module."
depends-on = ["../Vesper.Core"]

# Pure data, no inline IL and no BCL name, so the clr manifest names the same body.
files = [
    "result.fsi",
    "result.fs",
]
