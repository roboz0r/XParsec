[core]
name = "Vesper.Comparison"
description = "The ordering operators (< > <= >=)."
depends-on = ["../Vesper.Core"]

# Every declaration is `inline`, so this compiles to an empty DLL and splices at each
# use site.
files = [
    "comparison.fsi",
    "comparison.clr.fs",
]
