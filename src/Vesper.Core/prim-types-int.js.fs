namespace Vesper

#nowarn "42"

type sbyte = (# "number" #)
type byte = (# "number" #)
type int16 = (# "number" #)
type uint16 = (# "number" #)
type uint32 = (# "number" #)
type int64 = (# "bigint" #)
type uint64 = (# "bigint" #)
